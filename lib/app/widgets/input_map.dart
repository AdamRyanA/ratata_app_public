import 'package:flutter/material.dart';

class InputMap extends StatelessWidget {
  const InputMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
